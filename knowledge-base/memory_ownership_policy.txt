Northwind Memory Ownership Policy

Summary
	Aurora Runtime requires strict adherence to deterministic ownership. Resource ownership must be explicit and visible from type signatures.

Rules

	All plugin factories must return smart pointers.

	Ownership never changes unless transferred via std::move.

	Raw pointers may not persist longer than a single function call.

	Containers of polymorphic objects must use std::unique_ptr<Base>.